# docker-compose.yml
services:
  viewers:
    build: .
    ports:
      - "8001:80"
    environment:
      - API_BASE=${API_BASE:-http://localhost:8000}
    volumes:
      - ./app:/var/www/html   # live reload — no rebuild needed on file changes